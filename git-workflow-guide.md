# git-workflow-guide.md

---

# 🏗️ Git ও GitHub ব্যবহারের বিস্তারিত গাইডলাইন

### 1️⃣ Config (কনফিগারেশন)

Git-এ কাজ শুরু করার আগে পরিচয় সেট করে নিতে হয়, যাতে পরবর্তীতে বোঝা যায় প্রতিটি কোড কে লিখেছে বা পরিবর্তন করেছে।

```bash
git config --global user.name "Your Name"
```
👉 এটি দিয়ে গিটে নাম সেট করা হয়।

```bash
git config --global user.email "you@example.com"
```
👉 এটি দিয়ে ইমেইল অ্যাড্রেস সেট করা হয়। GitHub অ্যাকাউন্টের ইমেইল ব্যবহার করা উত্তম।

```bash
git config --list
```
👉 বর্তমানে সেট করা নাম, ইমেইল বা অন্যান্য কনফিগারেশনগুলো চেক করতে এটি ব্যবহৃত হয়।

---

### 2️⃣ Repository (রিপোজিটরি)

প্রজেক্টের যাবতীয় ফাইল এবং ইতিহাসের সংরক্ষাণাগারকে রিপোজিটরি বলা হয়।

```bash
git init
```
👉 সাধারণ কোনো ফোল্ডারকে গিটের আওতায় আনার জন্য বা নতুন লোকাল রিপোজিটরি তৈরি করতে এই কমান্ড ব্যবহার করা হয়।

```bash
git clone <url>
```
👉 অনলাইনে (যেমন GitHub-এ) থাকা কোনো প্রজেক্টের হুবহু কপি নিজের কম্পিউটারে নামিয়ে আনার জন্য এটি ব্যবহার করা হয়।

---

### 3️⃣ Branch (ব্রাঞ্চ বা শাখা)

মূল কোড ঠিক রেখে আলাদা আলাদা ফিচারের কাজ করার জন্য আলাদা শাখা বা ব্রাঞ্চ ব্যবহার করা হয়।

```bash
git branch
```
👉 প্রজেক্টে বর্তমানে কতগুলো ব্রাঞ্চ আছে এবং কোন ব্রাঞ্চে কাজ চলছে, তা দেখতে এটি ব্যবহার করা হয়।

```bash
git branch <name>
```
👉 নির্দিষ্ট নামে নতুন একটি ব্রাঞ্চ তৈরি করার জন্য এই কমান্ড ব্যবহৃত হয়।

```bash
git switch <name>
```
👉 এক ব্রাঞ্চ থেকে অন্য ব্রাঞ্চে যাওয়ার জন্য বা কাজের শাখা পরিবর্তন করতে এটি ব্যবহৃত হয়।

```bash
git switch -c <name>
```
👉 একসাথে নতুন একটি ব্রাঞ্চ তৈরি এবং সরাসরি সেই ব্রাঞ্চে প্রবেশ করার জন্য এটি শর্টকাট কমান্ড।

```bash
git merge <branch>
```
👉 অন্য কোনো ব্রাঞ্চের কাজ বর্তমান ব্রাঞ্চের সাথে যুক্ত বা একত্রিত করার প্রয়োজন হলে এটি ব্যবহৃত হয়।

```bash
git branch -d <name>
```
👉 কোনো ব্রাঞ্চের কাজ শেষ হয়ে গেলে সেটি লোকাল কম্পিউটার থেকে মুছে ফেলার জন্য।

```bash
git push origin --delete <name>
```
👉 অনলাইন বা রিমোট সার্ভার (GitHub) থেকে কোনো নির্দিষ্ট ব্রাঞ্চ ডিলিট করার জন্য।

---

### 4️⃣ Stage & Commit (পরিবর্তন সংরক্ষণ)

কোডে কোনো পরিবর্তন আনার পর সেটিকে স্থায়ীভাবে সেভ করার প্রক্রিয়া।

```bash
git status
```
👉 কোন কোন ফাইলে পরিবর্তন করা হয়েছে কিন্তু এখনো সেভ করা হয়নি, তার বর্তমান অবস্থা জানার জন্য এটি ব্যবহার করা হয়।

```bash
git add <file>
```
👉 শুধুমাত্র নির্দিষ্ট কোনো ফাইলের পরিবর্তনকে পরবর্তী ধাপের (Commit) জন্য প্রস্তুত বা 'Stage' করতে এটি ব্যবহৃত হয়।

```bash
git add .
```
👉 প্রজেক্টের সকল পরিবর্তিত ফাইলকে একসাথে 'Stage' করার জন্য এই কমান্ড ব্যবহৃত হয়।

```bash
git commit -m "Message"
```
---

## 5️⃣ Remote (GitHub)

নিজের কম্পিউটারে থাকা প্রজেক্টকে ইন্টারনেটে (যেমন GitHub-এ) ব্যাকআপ রাখা বা অন্যের সাথে কোড আদান-প্রদান করার প্রক্রিয়া।

```bash
git remote -v
```
👉 লোকাল রিপোজিটরির সাথে অনলাইনে কোন রিপোজিটরি যুক্ত আছে এবং তার ইউআরএল (URL) কী, তা যাচাই করার জন্য।

```bash
git remote add origin <url>
```
👉 লোকাল কম্পিউটারের প্রজেক্টের সাথে প্রথমবার অনলাইন বা রিমোট রিপোজিটরির একটি সংযোগ তৈরি করতে।

```bash
git push origin <branch>
```
👉 লোকাল কম্পিউটারে করা সব কাজ বা পরিবর্তনগুলো অনলাইনে (GitHub) নির্দিষ্ট ব্রাঞ্চে আপলোড করার জন্য।

```bash
git pull origin <branch>
```
👉 অনলাইন রিপোজিটরিতে থাকা নতুন কোনো পরিবর্তন বা আপডেট ডাউনলোড করে লোকাল ফাইলের সাথে যুক্ত (Merge) করার জন্য।

```bash
git fetch origin
```
👉 অনলাইন থেকে নতুন পরিবর্তনের তথ্যগুলো শুধু সংগ্রহ করার জন্য। এটি সরাসরি লোকাল ফাইল পরিবর্তন করে না।

---

## 6️⃣ History & Diff

প্রজেক্টের শুরু থেকে এ পর্যন্ত কী কী কাজ করা হয়েছে এবং ফাইলের কোথায় কী পরিবর্তন এসেছে তা জানার প্রক্রিয়া।

```bash
git log
```
👉 প্রজেক্টে এ পর্যন্ত যতগুলো সেভ (Commit) করা হয়েছে, সেগুলোর বিস্তারিত ইতিহাস দেখার জন্য।

```bash
git log --oneline
```
👉 অনেক বড় ইতিহাসকে সংক্ষেপে শুধু এক লাইনে দেখার জন্য।

```bash
git log --graph --all
```
👉 প্রজেক্টের বিভিন্ন ব্রাঞ্চ এবং সেগুলোর কাজের ইতিহাস গ্রাফ আকারে দেখার জন্য।

```bash
git diff
```
👉 কোড এডিট করার পর কিন্তু সেভ করার আগে, আগের কোডের সাথে বর্তমান কোডের পার্থক্য দেখার জন্য।

```bash
git show <commit_id>
```
👉 নির্দিষ্ট কোনো একটি কমিটে ঠিক কী কী ফাইল পরিবর্তন করা হয়েছিল তা দেখার জন্য।

```bash
git status -s
```
👉 প্রজেক্টের বর্তমান অবস্থা খুব সংক্ষেপে এক নজরে দেখার জন্য।

---

## 7️⃣ Undo / Reset

কাজ করার সময় কোনো ভুল হলে বা আগের কোনো অবস্থায় ফিরে যাওয়ার প্রয়োজন হলে এই কমান্ডগুলো ব্যবহৃত হয়।

```bash
git restore <file>
```
👉 কোনো ফাইলে পরিবর্তন করার পর সেটি সেভ করার আগে আগের অবস্থায় ফিরিয়ে আনার জন্য।

```bash
git restore --staged <file>
```
👉 কোনো ফাইল ভুল করে `git add` করলে সেটিকে unstaged অবস্থায় ফিরিয়ে আনার জন্য।

```bash
git reset --hard <commit>
```
👉 প্রজেক্টকে নির্দিষ্ট কোনো পুরনো কমিটের অবস্থায় পুরোপুরি ফিরিয়ে নেওয়া। (সতর্কতার সাথে ব্যবহার করতে হবে)

```bash
git revert <commit_id>
```
👉 পুরনো ভুল কমিটকে সংশোধন করে নতুন একটি কমিট তৈরি করার জন্য।

---

## 8️⃣ Stash

চলতি কাজ অসম্পূর্ণ থাকা অবস্থায় জরুরি অন্য কোনো কাজ করার জন্য বর্তমান পরিবর্তনগুলো সাময়িকভাবে লুকিয়ে রাখার প্রক্রিয়া।

```bash
git stash
```
👉 বর্তমানের অসম্পূর্ণ কাজগুলো সাময়িকভাবে সরিয়ে রাখা।

```bash
git stash list
```
👉 এ পর্যন্ত কতবার কাজ স্ট্যাশ করা হয়েছে তার তালিকা দেখার জন্য।

```bash
git stash apply
```
👉 স্ট্যাশে রাখা কাজগুলো আবার ফিরিয়ে আনার জন্য।

```bash
git stash drop
```
👉 স্ট্যাশে জমা রাখা কোনো কাজ স্থায়ীভাবে মুছে ফেলার জন্য।

---

## 9️⃣ Branch Workflow (আদর্শ কাজের ধারা)

**১. মেইন ব্রাঞ্চে যাওয়া যেখানে প্রজেক্টের প্রধান কোড থাকে**  
```bash
git switch main
```

**২. কাজ শুরু করার আগে অনলাইন থেকে সব লেটেস্ট আপডেট নামিয়ে নেওয়া**  
```bash
git pull origin main
```

**৩. নিজের নতুন কাজের জন্য একটি আলাদা শাখা বা ব্রাঞ্চ তৈরি করা**  
```bash
git switch -c feature-branch
```

**৪. কোডিং শেষ হলে পরিবর্তিত ফাইলগুলো সেভ করার জন্য প্রস্তুত করা**  
```bash
git add .
```

**৫. কী কাজ করা হয়েছে তার নোটসহ স্থায়ীভাবে সেভ করা**  
```bash
git commit -m "Feature added"
```

**৬. নিজের তৈরি করা ব্রাঞ্চটি অনলাইনে পাঠিয়ে দেওয়া**  
```bash
git push origin feature-branch
```

---

💡 **শেষ ধাপ:**  
GitHub-এ গিয়ে একটি **Pull Request (PR)** তৈরি করতে হয়, যাতে টিমের অন্যরা আপনার কাজ রিভিউ করে মেইন ব্রাঞ্চে merge করতে পারে।  

---
